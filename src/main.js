let reactBar = document.getElementById("react-bar");
let vueBar = document.getElementById("vue-bar");
let nodeBar = document.getElementById("node-bar");
let mongoBar = document.getElementById("mongo-bar");
let apiBar = document.getElementById("api-bar");
let cypressBar = document.getElementById("cypress-bar");

RPGUI.set_value(reactBar, 0.5);
RPGUI.set_value(vueBar, 0.5);
RPGUI.set_value(nodeBar, 0.5);
RPGUI.set_value(mongoBar, 0.5);
RPGUI.set_value(apiBar, 0.5);
RPGUI.set_value(cypressBar, 0.5);
